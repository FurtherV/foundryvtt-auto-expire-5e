{
  "scripts": {
    "build": "npm run build:css",
    "build:css": "lessc less/auto-expire-5e.less styles/auto-expire-5e.css",
    "lint": "eslint --ext .mjs --ignore-path .gitignore ./",
    "lint:fix": "eslint --ext .mjs --ignore-path .gitignore ./ --fix",
    "prettier": "prettier . --check",
    "prettier:fix": "prettier . --write",
    "watch": "less-watch-compiler less styles auto-expire-5e.less"
  },
  "devDependencies": {
    "less": "^4.2.0",
    "less-watch-compiler": "^1.16.3",
    "prettier": "^3.2.5"
  }
}
